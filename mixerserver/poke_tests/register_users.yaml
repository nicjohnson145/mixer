vars:
  url: "http://localhost:8080/api/v1/register-user"

calls:
- name: create-foo
  url: '{{ .url }}'
  body:
    username: foo
    password: foopass
- name: create-bar
  url: '{{ .url }}'
  body:
    username: bar
    password: barpass
- name: create-baz
  url: '{{ .url }}'
  body:
    username: baz
    password: bazpass
